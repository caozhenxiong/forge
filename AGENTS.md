# Forge Agent Agreements

这份文件定义 `Forge` 仓库中的工作约定。后续 agent 在分析、设计、编码、重构、评审、测试前，都必须先过一遍这里的约束。

## 核心原则

- 先判断整体思路、抽象边界、流程方向是否正确，再决定是否改代码。
- 如果发现方向错误，优先重构思路和边界，不在错误道路上继续修修补补。
- 解决问题必须解决“一类问题”的根因，不做场景特判，不做关键词补丁，不做项目级硬编码。
- 优先保证优雅性、可复用性、可扩展性、可维护性，而不是只追求当前案例表面跑通。
- 修改前要先判断这次改动是在修根因，还是只是在修表象。
- 遇到明显偏工具属性且实现复杂的能力，先做 build-vs-buy 判断，优先检查是否已有合适类库、框架或现成方案，再决定是否自研。
- 当用户主动提出方案时，如果判断存在更优的整体方案或更稳的实现路径，必须明确提出异议、说明理由，并给出替代方案；不允许明知方案较差却机械顺着执行。

## 代码约定

- 复杂流程、关键边界、恢复链、阶段 gate、契约判定必须补充中文高价值注释。
- 注释必须解释“为什么这样做、边界是什么、不负责什么”，不要写低信息量废话注释。
- 任何代码逻辑变更都必须同步更新对应文档，避免实现、设计备忘、工作流规则和路线图长期失真。
- 默认避免继续制造超大类、全能类、超长方法；优先按职责拆分。
- 避免把 prompt、流程策略、语言文案、恢复逻辑、场景规则揉进同一个类。
- 避免整文件 round-trip 生成；优先小块生成、本地组装、本地校验。
- 不要用正则或字符串关键词去猜大模型自由文本的语义；对 agent 输出的语义判断，优先要求模型返回结构化字段，再由确定性 policy 消费。
- 不允许把自然语言 regex/关键词匹配作为流程推进、gate 判定或失败分流的主依据；固定协议解析可以保留，但自由文本语义必须结构化。
- 不要继续引入魔法数字和魔法字符串，尤其不要把协议 key、metadata key、tag、reason code、阈值散落在代码里重复硬写。
- 稳定协议 key、marker、reason code、阶段名、artifact key 必须集中到常量/spec/policy/config 中，不允许在多个类里各写一份。
- 对外协议枚举值优先收成 enum/spec，对内共享动作、case type、metric key、阶段动作等稳定值不允许继续散落硬写。
- 可调阈值、重试次数、预算上限、默认策略优先进入 policy/config，不要直接写死在流程代码里。
- 每一轮集中清理后都必须重新全局复扫；只要还存在新的 prose 语义猜测或散落字面量，就继续改，不允许“先留着以后再说”。
- 禁止以“过渡实现”“先跑起来再说”为理由，把明知错误的抽象、临时兼容层、半套新旧并存主链带入主代码路径。
- 禁止为了先打通链路而引入场景硬编码、临时阈值、关键词猜测或其他注定要回头清理的补丁；如果只能靠这类方式推进，必须先停下来重做抽象或方案。
- 任何“为了降低风险而暂时保留”的逻辑，都视为未完成，而不是稳妥；只要主链还依赖这类保留逻辑，本次任务不得宣称完成，也不得进入下一阶段。
- 任何“后续再清理”的说法，都视为本次任务失败；如果某类问题需要后续单独清理，说明这次收口没有完成，必须继续在当前任务内清干净。
- 每次代码改动完成后，必须先做自测，再做一次代码 review；没有 `self-test + code review`，任务不得宣称完成。

## 失败判定

- 任何“为了降低风险而暂时保留”的逻辑，都直接判定为未完成，不得解释为稳妥推进。
- 任何“后续再清理”的安排，都直接判定为本次任务失败，不得作为提交半成品实现的理由。
- 任何代码变更如果没有完成 `self-test + code review`，都直接判定为未完成，不得进入下一阶段。

## Prompt 与流程约定

- Prompt 设计要解决一类问题，不要针对当前场景堆规则。
- 管理“信息来源、约束等级、升级规则”，不要把推断、建议、设计选择伪装成硬约束。
- 文档、review、test、repair 的规则要尽量通用，避免按“网页/游戏/某个案例”写专门逻辑。
- coder 必须能够根据指令真正实现，骨架、占位、半成品不能冒充完成。
- review 与 test 不能放过阻塞性能力缺口；阶段完成必须与真实交付状态一致。

## 每次动手前的检查清单

- 当前问题是具体 bug，还是整体思路错误？
- 这次改动有没有引入新的场景特判或硬编码？
- 职责边界是否比之前更清晰、更优雅？
- 这项复杂能力是否其实更适合复用现成类库或工具，而不是继续手搓？
- 用户当前提出的方案是否存在更优替代？如果有，是否已经明确提出反驳和替代方案？
- 是否补了必要的中文注释？
- 这次逻辑变更对应的文档是否已经同步更新？
- 是否提升了可维护性，而不是继续堆冗余？
- 是否有更小、更稳定的抽象可以替代当前补丁？
- 这次是否又在用 regex/关键词匹配去猜模型语义？如果是，能否改成结构化输出 + 确定性判断？
- 这次是否又引入了新的魔法数字或魔法字符串？相关 key、阈值、marker 是否已经集中管理？
- 这轮改完之后是否已经重新全局复扫，并确认没有留下新的自然语言语义猜测或散落协议字面量？
- 这轮代码改完之后是否已经完成 `self-test + code review`？

## 持续要求

- 持续关注整条链路：
  - 文档
  - contract
  - projected/task memory
  - implementation
  - review
  - test
  - run 状态
- 在黄金路径未跑通前，优先做稳定化与收敛，不盲目扩能力。
